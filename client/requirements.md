## Packages
framer-motion | Page transitions and complex animations
lucide-react | Beautiful icons for the UI
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
zod | Schema validation
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
The backend provides `/api/analyze` which takes a complex object including diseases, allergies, and available ingredients.
We need to visualize nutritional data clearly.
The design should be "medical-aware" but friendly - soft greens, blues, clean typography.
